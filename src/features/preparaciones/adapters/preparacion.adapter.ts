import type { Preparacion } from "../types/preparacion.type";

/* ============================
   ADAPT FROM API
============================ */
export const adaptPreparacionFromApi = (
  api: Preparacion
): Preparacion => {
  return api
};